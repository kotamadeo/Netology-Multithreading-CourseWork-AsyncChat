package com.gmail.at.kotamadeo.connection;

public enum MessageType {
    REQUEST_NICKNAME,
    TEXT_MESSAGE,
    NICKNAME_ACCEPTED,
    NICKNAME,
    NICKNAME_USED,
    USER_ADDED,
    DISABLE_USER,
    REMOVED_USER,
    PRIVATE_TEXT_MESSAGE,
    NICKNAME_CHANGED,
}
